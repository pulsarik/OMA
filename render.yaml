services:
  - type: web
    name: omaha-hi-lo-pov
    runtime: node
    plan: free
    buildCommand: npm --prefix server ci && npm --prefix demo/client ci && node server/scripts/write-build-info.cjs && npm --prefix server run build && npm --prefix demo/client run build
    startCommand: node server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
